# Классификация рекуррентных соотношений

>[!definition] Рекуррентное соотношение k-го порядка
>*Рекуррентное соотношение $k$-го порядка:* $f(n) = F(n, f(n-1), \dots, f(n-k))$
>
>здесь $F$ - «нормальная» $(k+1)$-местная функция
>для задания функции таким соотношением нужно $k$ начальных значений $f(0),\dots,f(k-1)$

>[!example]- Примеры:
>Числа Фибоначчи - соотношение *второго порядка*
>факториал, логистическая функция, ханойская башня - *первого порядка*
>не являются соотношениями $k$-го порядка
>соотношение из задачи про интервальные расписания ($p_{n}$ может быть любым)
>соотношения вида $f(n) = f\left( \left\lfloor  \frac{n}{2}  \right\rfloor\right) + f\left( \left\lceil  \frac{n}{2}  \right\rceil \right) + n$
>- возникают при оценке сложности *рекурсивных алгоритмов*

>[!definition] Определения:
>Рекуррентное соотношение $f(n) = F(n, f(n-1), \dots, f(n-k))$ называется
>
>*линейным*, если $F=F(x_{1},\dots,x_{k})$ - линейная функция с коэффициентами, зависящими от параметра $n$
>- т.е. $f(n) = a_{1}(n)f(n-1) + \dots + a_{k}f(n-k) + a(n)$
>*линейным однородным*, если $a(n) = 0$
>*линейным с постоянными коэффициентами*, если все $a_{i}(n)$ - константы
>- $a(n)$ может не быть константой

^e81301

>[!example] Примеры:
>логистическая функция $f(n) = r \cdot f(n-1)(1 - f(n-1))$ : нелинейное
>
>факториал $f(n) = n \cdot f(n-1)$ : линейное однородное с переменными коэффициентами
>
>ханойская башня $f(n) = 2 \cdot f(n-1) + 1$ : линейное неоднородное с постоянными коэффициентами
>
>числа Фибоначчи $f(n) = f(n-1) + f(n-2)$ : линейное однородное с постоянными коэффициентами